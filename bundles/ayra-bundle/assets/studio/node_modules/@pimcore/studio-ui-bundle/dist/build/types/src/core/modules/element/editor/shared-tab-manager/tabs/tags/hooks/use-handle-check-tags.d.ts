/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import { type Key } from 'react';
import { type Tag } from '../../../../../../../modules/element/editor/shared-tab-manager/tabs/tags/tags-api-slice-enhanced';
interface UseHandleCheckProps {
    elementId: number;
    elementType: 'asset' | 'document' | 'data-object';
    flatTags: Tag[];
    setDefaultCheckedTags: (tags: React.Key[]) => void;
}
interface NodeInfo {
    node: {
        key: Key;
    };
    checked: boolean;
}
interface UseHandleCheckReturn {
    handleCheck: (checkedKeys: {
        checked: Key[];
        halfChecked: Key[];
    }, info: NodeInfo) => Promise<void>;
    loadingNodes: Set<string>;
}
export declare const useHandleCheck: ({ elementId, elementType, flatTags, setDefaultCheckedTags }: UseHandleCheckProps) => UseHandleCheckReturn;
export {};
