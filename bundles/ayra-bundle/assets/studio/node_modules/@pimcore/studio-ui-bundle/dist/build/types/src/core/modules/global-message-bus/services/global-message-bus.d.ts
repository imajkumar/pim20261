/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import { type BackgroundProcessor } from '../../../modules/background-processor/services/background-processor';
import { type AbstractMessageHandler } from '../message-handlers/abstract-message-handler';
import { type AbstractMercureMessage } from '../../../modules/background-processor/process/abstract-mercure-process';
export declare class GlobalMessageBus {
    private readonly backgroundProcessor;
    private readonly activeHandlers;
    private globalSubscriptionId;
    private readonly registeredTopics;
    private messageBuffer;
    private readonly MESSAGE_BUFFER_TTL;
    private readonly MAX_BUFFER_SIZE;
    private readonly debouncedCleanup;
    constructor(backgroundProcessor: BackgroundProcessor);
    /**
     * Register topics that handlers can use
     * @throws Error if global subscription has already started
     */
    registerTopics(topics: string[]): void;
    getRegisteredTopics(): string[];
    registerHandler(handler: AbstractMessageHandler): void;
    unregisterHandler(handlerId: string | number): void;
    startGlobalSubscription(): void;
    routeMessage(mercureMessage: AbstractMercureMessage): Promise<void>;
    private bufferMessage;
    private replayBufferedMessages;
    private cleanupExpiredMessages;
}
