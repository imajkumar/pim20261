/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import { type ActiveTabContextProps, type WidgetEditorActionsContextProps, type WidgetEditorDataContextProps } from '../widget-editor-provider';
/** Returns only the widgets data. Re-renders when widgets array changes. */
export declare const useWidgetEditorData: () => WidgetEditorDataContextProps;
/** Returns only action functions (setWidgets, openWidget, closeWidget, createWidget).
 *  These are stable references — this hook will NOT cause re-renders on widgets/tab changes. */
export declare const useWidgetEditorActions: () => WidgetEditorActionsContextProps;
/** @deprecated Use useWidgetEditorData() + useWidgetEditorActions() for better performance.
 *  This hook subscribes to BOTH data and actions, causing re-renders on any widgets change. */
export declare const useWidgetEditorContext: () => WidgetEditorDataContextProps & WidgetEditorActionsContextProps;
export declare const useActiveTabContext: () => ActiveTabContextProps;
