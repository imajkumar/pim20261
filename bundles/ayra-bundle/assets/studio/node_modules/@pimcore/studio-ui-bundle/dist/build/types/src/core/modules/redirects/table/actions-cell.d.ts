/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import React from 'react';
import { type CellContext } from '@tanstack/react-table';
import { type RedirectRow } from '../hooks/use-redirects';
type RedirectWithActions = RedirectRow & {
    actions: React.ReactNode;
};
interface ActionsCellProps {
    info: CellContext<RedirectWithActions, React.ReactNode>;
    setRedirectRows: React.Dispatch<React.SetStateAction<RedirectRow[]>>;
}
export declare const ActionsCell: ({ info, setRedirectRows }: ActionsCellProps) => React.JSX.Element;
export {};
