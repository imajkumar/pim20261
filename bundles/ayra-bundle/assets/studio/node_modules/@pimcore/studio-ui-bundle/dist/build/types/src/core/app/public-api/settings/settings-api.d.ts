/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import { type SystemSettingsGetApiResponse } from '../../../modules/app/settings/settings-slice.gen';
import { type store } from '../../../app/store';
/**
 * Settings API for iframe communication
 * Provides access to system settings from parent window to iframes
 */
export declare class SettingsApi {
    private store;
    /**
     * Initialize the settings API with the Redux store
     * This should be called during app initialization
     */
    initialize(appStore: typeof store): void;
    /**
     * Get current system settings
     * Returns the settings from the Redux store if available
     */
    getSettings(): SystemSettingsGetApiResponse | null;
    /**
     * Check if settings are available
     */
    areSettingsAvailable(): boolean;
}
export declare const settingsApi: SettingsApi;
