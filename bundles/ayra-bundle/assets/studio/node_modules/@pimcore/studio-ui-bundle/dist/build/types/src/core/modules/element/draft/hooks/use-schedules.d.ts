/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import type { ActionCreatorWithPayload, PayloadAction } from '@reduxjs/toolkit';
import type { EntityAdapter, EntityState } from '@reduxjs/toolkit/src/entities/models';
import { type Schedule as ApiSchedule } from '../../../../modules/element/editor/shared-tab-manager/tabs/schedule/schedule-api-slice-enhanced';
import { type TrackableChangesDraft } from '../../../../modules/element/draft/hooks/use-trackable-changes';
export interface ScheduleAction {
    id: number;
    schedule: Schedule;
}
export interface SchedulesAction {
    id: number;
    schedules: Schedule[];
}
export type Schedule = ApiSchedule & {
    archived: boolean;
};
export interface SchedulesDraft extends TrackableChangesDraft {
    schedules: Schedule[];
}
interface UseSchedulesReturn {
    addSchedule: (state: EntityState<SchedulesDraft, number>, action: PayloadAction<ScheduleAction>) => void;
    removeSchedule: (state: EntityState<SchedulesDraft, number>, action: PayloadAction<ScheduleAction>) => void;
    updateSchedule: (state: EntityState<SchedulesDraft, number>, action: PayloadAction<ScheduleAction>) => void;
    setSchedules: (state: EntityState<SchedulesDraft, number>, action: PayloadAction<SchedulesAction>) => void;
    resetSchedulesChanges: (state: EntityState<SchedulesDraft, number>, action: PayloadAction<number>) => void;
}
export declare const useSchedulesReducers: (entityAdapter: EntityAdapter<SchedulesDraft, number>) => UseSchedulesReturn;
export interface UseSchedulesDraftReturn {
    schedules: undefined | Schedule[];
    updateSchedule: (schedule: Schedule) => void;
    addSchedule: (schedule: Schedule) => void;
    removeSchedule: (schedule: Schedule) => void;
    setSchedules: (schedules: Schedule[]) => void;
    resetSchedulesChanges: () => void;
}
export declare const useSchedulesDraft: (id: number, draft: SchedulesDraft, updateScheduleAction: ActionCreatorWithPayload<ScheduleAction>, addScheduleAction: ActionCreatorWithPayload<ScheduleAction>, removeScheduleAction: ActionCreatorWithPayload<ScheduleAction>, setSchedulesAction: ActionCreatorWithPayload<SchedulesAction>, resetSchedulesChangesAction: ActionCreatorWithPayload<number>) => UseSchedulesDraftReturn;
export {};
