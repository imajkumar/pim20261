/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import React from 'react';
import 'leaflet/dist/leaflet.css';
import 'leaflet-draw';
import 'leaflet-draw/dist/leaflet.draw.css';
import { type GeoType } from '../../components/geo-map/types/geo-types';
export type GeoMapMode = 'geoPoint' | 'geoPolyLine' | 'geoPolygon' | 'geoBounds';
interface GeoMapProps {
    onChange?: (value?: GeoType) => void;
    mode?: GeoMapMode;
    value?: GeoType;
    width?: string;
    height?: string;
    lat?: number;
    lng?: number;
    zoom?: number;
    disabled?: boolean;
}
export interface GeoMapAPI {
    forceRerender: () => void;
    reset: () => void;
    setLat: (lat: number) => void;
    setLng: (lng: number) => void;
    setZoom: (zoom: number) => void;
    setValue: (value?: GeoType) => void;
}
declare const GeoMap: React.ForwardRefExoticComponent<GeoMapProps & React.RefAttributes<GeoMapAPI>>;
export { GeoMap };
