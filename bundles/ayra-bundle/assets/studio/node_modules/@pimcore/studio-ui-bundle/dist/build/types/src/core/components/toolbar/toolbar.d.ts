/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import { type FlexProps } from 'antd';
import React from 'react';
import { type BoxProps } from '../../components/box/box';
export interface ToolbarProps {
    children: React.ReactNode;
    justify?: FlexProps['justify'];
    align?: FlexProps['align'];
    theme?: 'primary' | 'secondary';
    borderStyle?: 'default' | 'primary';
    position?: 'top' | 'bottom' | 'content';
    size?: 'small' | 'auto' | 'default';
    padding?: BoxProps['padding'];
    margin?: BoxProps['margin'];
}
export declare const Toolbar: ({ children, size, justify, align, theme, position, borderStyle, padding, margin, ...props }: ToolbarProps) => React.JSX.Element;
