/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import React from 'react';
import { type SiteFormValues } from '../site-form';
interface SiteModalProviderProps {
    children: React.ReactNode;
}
export interface SiteModalContextProps {
    openModal: (config: SiteModalConfig) => void;
    closeModal: () => void;
    isOpen: boolean;
    currentDocumentId: number | null;
}
export interface SiteModalConfig {
    title: string;
    documentId: number;
    documentPath?: string;
    initialValues: SiteFormValues;
    onSubmit: (values: SiteFormValues) => Promise<void>;
}
export declare const SiteModalContext: React.Context<SiteModalContextProps | undefined>;
export declare const SiteModalProvider: ({ children }: SiteModalProviderProps) => React.JSX.Element;
export {};
