/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import React from 'react';
import { type PickerProps } from 'antd/lib/date-picker/generatePicker/interface';
import { type DatePickerValueType, type OutputType } from './utils/date-picker-utils';
import { type DateRangePickerProps } from '../../components/date-picker/date-range-picker';
import { type TimePickerProps } from '../../components/date-picker/time-picker';
export type DatePickerProps = PickerProps & {
    value?: DatePickerValueType;
    onChange?: (date: DatePickerValueType) => void;
    outputType?: OutputType;
    outputFormat?: string;
    disabled?: boolean;
    inherited?: boolean;
    showSuffixIcon?: boolean;
};
interface DatePickerReturn extends React.FC<DatePickerProps> {
    RangePicker: React.FC<DateRangePickerProps>;
    TimePicker: React.FC<TimePickerProps>;
}
export declare const DatePicker: DatePickerReturn;
export {};
