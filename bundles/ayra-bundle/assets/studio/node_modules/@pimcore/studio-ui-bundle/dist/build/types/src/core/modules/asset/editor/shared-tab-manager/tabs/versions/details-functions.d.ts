/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import { type CustomMetadataVersion, type AssetVersion } from '../../../../../../modules/element/editor/shared-tab-manager/tabs/versions/version-api-slice-enhanced';
export interface AssetVersionData {
    versionCount: number;
    baseDataFormatted: {
        fileName: string;
        /** creation date */
        creationDate: string;
        /** modification date */
        modificationDate: string;
        /** file size */
        fileSize: string;
        /** mime type */
        mimeType: string;
        /** dimensions */
        dimensions: string;
    };
    /** Metadata */
    metadata: Map<string, AssetVersionMetadata>;
    previewImageUrl: string | null;
    dataRaw: AssetVersion;
}
export interface AssetVersionMetadata {
    key: string;
    field: string;
    metadataType: string;
    displayValue: JSX.Element | string;
    language?: string;
    raw: CustomMetadataVersion;
}
export declare const hydrateVersionData: (dataRaw: AssetVersion, versionId: number, versionCount: number) => AssetVersionData;
export declare const checkIsImageVersion: (version?: AssetVersion) => boolean;
export declare const loadPreviewImage: (version: AssetVersion, versionId: number) => Promise<string | null>;
export declare const versionsDataToTableData: (data: AssetVersionData[]) => object[];
