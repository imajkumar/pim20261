/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import React from 'react';
import { type ElementIcon } from '../icon/icon';
export interface DragHandleProps {
    listeners?: Record<string, any>;
}
export interface ToolStripProps {
    className?: string;
    children?: React.ReactNode;
    theme?: 'default' | 'inverse';
    dragger?: boolean | DragHandleProps;
    title?: string;
    activateOnHover?: boolean;
    rounded?: boolean;
    disabled?: boolean;
    additionalIcon?: string | ElementIcon;
    additionalIconPosition?: 'before' | 'after';
    /**
     * Enable automatic theme-based icon coloring (matches title text).
     * Defaults to `false` (uses button default color). Ignored when ElementIcon.colorToken is set.
     */
    additionalIconAutoColor?: boolean;
}
export declare const ToolStrip: ({ children, className, theme: toolStripTheme, dragger, title, activateOnHover, rounded, disabled, additionalIcon, additionalIconPosition, additionalIconAutoColor }: ToolStripProps) => React.JSX.Element;
