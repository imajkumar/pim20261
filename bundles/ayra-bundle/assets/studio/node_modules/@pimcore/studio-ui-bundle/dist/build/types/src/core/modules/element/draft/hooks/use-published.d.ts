/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import type { ActionCreatorWithPayload, PayloadAction } from '@reduxjs/toolkit';
import type { EntityAdapter, EntityState } from '@reduxjs/toolkit/src/entities/models';
export interface PublishedDraft {
    published: boolean;
}
interface UsePublishedDataReducersReturn {
    publishDraft: (state: EntityState<PublishedDraft, number>, action: PayloadAction<{
        id: number;
    }>) => void;
    unpublishDraft: (state: EntityState<PublishedDraft, number>, action: PayloadAction<{
        id: number;
    }>) => void;
}
export declare const usePublishedReducers: (entityAdapter: EntityAdapter<PublishedDraft, number>) => UsePublishedDataReducersReturn;
export interface UsePublishedData {
    publishDraft: () => void;
    unpublishDraft: () => void;
}
export declare const usePublishedDraft: (id: number, publishDraftAction: ActionCreatorWithPayload<{
    id: number;
}>, unpublishDraftAction: ActionCreatorWithPayload<{
    id: number;
}>) => UsePublishedData;
export {};
