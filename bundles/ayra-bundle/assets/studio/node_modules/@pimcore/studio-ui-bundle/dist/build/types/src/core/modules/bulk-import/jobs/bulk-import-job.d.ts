/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import { type JobInterface, type JobRunOptions } from '../../../modules/execution-engine/jobs/job-interface';
import { MessageBusJobHandler } from '../../../modules/execution-engine/message-handlers/message-bus-job/message-bus-job-handler';
import { type BulkImportItem } from '../components/bulk-import-modal/context/bulk-import-context';
import { type JobRunList } from '../../../modules/execution-engine/services/job-rehydration-registry';
export interface BulkImportJobOptions {
    fileId: string;
    items: BulkImportItem[];
    title: string;
    onFinish?: () => void;
}
export declare class BulkImportJob implements JobInterface {
    static readonly jobNames: readonly ["studio_ee_job_bulk_import_class_definitions"];
    static rehydrate(jobRuns: JobRunList): MessageBusJobHandler;
    private readonly fileId;
    private readonly items;
    private readonly title;
    private readonly onFinish?;
    constructor(options: BulkImportJobOptions);
    run(options: JobRunOptions): Promise<void>;
}
