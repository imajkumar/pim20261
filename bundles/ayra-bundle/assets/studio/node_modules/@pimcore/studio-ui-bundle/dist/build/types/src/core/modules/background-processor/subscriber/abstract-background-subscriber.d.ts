/**
 * This source file is available under the terms of the
 * Pimcore Open Core License (POCL)
 * Full copyright and license information is available in
 * LICENSE.md which is distributed with this source code.
 *
 *  @copyright  Copyright (c) Pimcore GmbH (https://www.pimcore.com)
 *  @license    Pimcore Open Core License (POCL)
 */
import { type AbstractMessage } from '../process/abstract-background-process';
export type SubscriberConstructor = new (callback: (message: AbstractMessage) => void) => AbstractBackgroundSubscriber;
export declare abstract class AbstractBackgroundSubscriber {
    protected readonly id: string;
    protected callback: (message: AbstractMessage) => void;
    constructor(callback: (message: AbstractMessage) => void);
    getId(): string;
    getCallback(): (message: AbstractMessage) => void;
}
